services:
  jobalert:
    build: .
    image: job-alert-agent:local
    volumes:
      - ./config:/app/config:ro
      - ./secrets:/app/secrets
      - ./data:/app/data
      - ./reports:/app/reports
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
